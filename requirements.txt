# Multilingual Indian Language Chatbot - Dependencies
# Core
fastapi==0.115.0
uvicorn==0.30.0
pydantic>=2.0

# LLM Engine
ollama>=0.4.0

# Cloud LLM fallback (used automatically if Ollama isn't running)
groq>=0.11.0
python-dotenv>=1.0.0

# Translation
deep-translator>=1.11.0

# Language Detection
fasttext-wheel>=0.9.2
# fasttext's predict() is broken on NumPy 2.x (ValueError: "Unable to avoid
# copy while creating an array"). Pin numpy<2 until fasttext fixes this upstream.
numpy<2

# Voice
SpeechRecognition>=3.10.0
gTTS>=2.5.0
pyttsx3>=2.90

# Template & Multipart
jinja2>=3.1.0
python-multipart>=0.0.6

# HTTP Client
requests>=2.28.0
